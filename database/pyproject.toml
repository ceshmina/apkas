[project]
name = "database"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13.1"
dependencies = []

[dependency-groups]
dev = [
    "alembic>=1.14.1",
    "sqlalchemy-aurora-data-api>=0.5.0",
]
