name: Release public

on:
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  test:
    uses: ./.github/workflows/_test_public.yml

  deploy:
    needs: test
    uses: ./.github/workflows/_deploy_public.yml
    with:
      role-to-assume: arn:aws:iam::961341543806:role/github-actions
      s3-bucket-name: apkas-production-frontend
      cloudfront-distribution-id: E2VSZEN4DJKC3O
